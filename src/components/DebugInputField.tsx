type DebugInputFieldProps = {};

/**
 * Used to add events to the schedule.
 * We won't be adding events like this in the real version.
 * That will be done another way.
 */
const DebugInputField = ({}: DebugInputFieldProps) => {
  return <div className="bg-stroke p-4 rounded text-primary"></div>;
};

export default DebugInputField;
