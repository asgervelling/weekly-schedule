import { CalendarEvent } from "../../types";

/**
 * h_p = m_e / m_d * h_d \
 * where \
 * h_p = height of panel \
 * m_e = minutes in event \
 * m_d = minutes in day \
 * h_d = height of day
 */
// export const panelHeight = (e: CalendarEvent): number => {
//   m_e = 
//   return Math.round()
// };